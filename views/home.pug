extends layout

block content
  .w-100.p-3.escape-container
    #map(style='width:110vw; min-height: 300px; height: 95vh; position:absolute; margin-top: -30px; margin-left: -10px; overflow-x: hidden;')